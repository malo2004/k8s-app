apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
   run: nginx
spec:
  type: NodePort
  ports:
  - port: 8080
    targetport: 80
    protocol: tcp
    name: http
  selector:
    run: nginx 
